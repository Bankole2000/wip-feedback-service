import fiveEmojiRating from "./emoji5.json";
import fiveStarRating from "./star5.json";

const rating = {
  desc: "User responds based on a point scale from least to greatest",
  name: "rating Scale Templates",
  templates: {
    fiveEmojiRating,
    fiveStarRating,
  },
};

export { rating };
