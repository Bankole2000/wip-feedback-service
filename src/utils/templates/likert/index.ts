import { fivePoint } from "./five";
import { sevenPoint } from "./seven";

const likert = {
  desc: "User responds based on a point scale from least to greatest",
  name: "Likert Scale Templates",
  fivePoint,
  sevenPoint,
};

export { likert };
